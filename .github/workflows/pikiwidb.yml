name: Pikiwidb

on:
  # 为了方便测试，目前不指定分支就可以触发
  push:
  pull_request:

jobs:
  build_on_macos:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          sh build.sh