name: Pikiwidb

on:
  push:
    branches: [ "unstable" ]
  pull_request:
    branches: [ "unstable" ]

jobs:
  build_on_macos:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          sh build.sh